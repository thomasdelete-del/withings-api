services:
  withings-api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_PATH: /data/withings.db
      BASE_URL: http://localhost:8000
    volumes:
      - withings-data:/data
    restart: unless-stopped

volumes:
  withings-data:
